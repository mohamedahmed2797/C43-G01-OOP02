namespace assignment_2_oop
{
    internal class Program
    {
        static void Main(string[] args)
        {
            #region 1.	Define a struct "Person" with properties "Name" and "Age". Create an array of three "Person" objects and populate it with data. Then, write a C# program to display the details of all the persons in the array.
            //Person[] person = new Person[3];
            //person[0] = new Person(name: "mohamed ahmed", age: 23);
            //person[1] = new Person(name: "ahmed kamal", age: 50);
            //person[2] = new Person(name: "mostafa mohamed", age: 27);
            //foreach (Person p in person)
            //{
            //    Console.WriteLine($"Name : {p.Name} \t age : {p.Age}");
            //} 
            #endregion
            #region 2.	Create a struct called "Point" to represent a 2D point with properties "X" and   "Y". Write a C# program that takes two points as input from the user and calculates the distance between them.
            //Point point = new Point();
            //Console.Write("enter x1: ");
            //point.X1 = Convert.ToInt32(Console.ReadLine());
            //Console.Write("enter y1: ");
            //point.Y1 = Convert.ToInt32(Console.ReadLine());
            //Console.Write("enter x2: ");
            //point.X2 = Convert.ToInt32(Console.ReadLine());
            //Console.Write("enter y2: ");
            //point.Y2 = Convert.ToInt32(Console.ReadLine());
            //int distancePower2 = ((point.X2 - point.X1) * (point.X2 - point.X1)) + ((point.Y2 - point.Y1) * (point.Y2 - point.Y1));
            //double distance = Math.Sqrt(distancePower2);
            //Console.WriteLine($"the distance between {point.X1} ,{point.Y1} , {point.X2} , {point.Y2} is : {distance} "); 
            #endregion
            #region 3.	Create a struct called "Person" with properties "Name" and "Age". Write a C# program that takes details of 3 persons as input from the user and displays the name and age of the oldest person.
            //Person[] person = new Person[3];
            //for (int i = 0; i < person.Length; i++)
            //{
            //    Console.WriteLine($"person {i+1}  \n");
            //    Console.WriteLine("enter your name");
            //    string name = Console.ReadLine();
            //    Console.WriteLine("enter your age");
            //    int age = Convert.ToInt32( Console.ReadLine());
            //    person[i] = new Person (name, age);
            //}
            //Person old = person[0];
            //for (int i = 1; i < person.Length; i++)
            //{
            //  if (person[i].Age > old.Age)
            //    {
            //        old = person[i];
            //    }
            //}
            //Console.WriteLine($"the oldest person \n name is {old.Name} \n age is {old.Age}");
            #endregion
            #region 4.     Create a struct named Rectangle that represents a rectangle with the following fields:width(type: double) height(type: double)
            //Rectangle rectangle = new Rectangle();
            //Console.Write("enter rectangle width: ");
            //rectangle.Width = Convert.ToDouble( Console.ReadLine());
            //Console.Write("enter rectangle height: ");
            //rectangle.Height = Convert.ToDouble(Console.ReadLine());
            //rectangle.DisplayInfo();
            #endregion
        }
    }
}
